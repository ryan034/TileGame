/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class StatsPanel : MonoBehaviour
{

    public Text text;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*
        string text_ = "";
        foreach (Unit unit in Tilemanager.globalinstance.units)
        {
            text_ = text_ + unit.name + " " + unit.currenthp.ToString()+"/"+unit.hp.ToString() + "\n";
        }
        foreach (Building building in Tilemanager.globalinstance.buildings)
        {
            text_ = text_ + building.name + " " + building.currenthp.ToString() + "/" + building.hp.ToString() + "\n";
        }
        text.text = text_;

    }
}*/
